package com.seongja.jarvis

/**
 * Keeps Android activity constants available to service-only source files while
 * remaining exactly the platform Activity type for activities in this package.
 */
typealias Activity = android.app.Activity
