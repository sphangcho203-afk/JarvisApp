"""Base FRIDAY sensory integration has been committed directly.

This retained no-op keeps older CI references harmless while preventing stale
patch anchors from rewriting newer source. Pending integrations live in
`run_pending_integrations.py` until each is committed, tested, and retired.
"""

if __name__ == "__main__":
    print("Base sensory source is already integrated.")
